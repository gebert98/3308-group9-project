<div class="container mt-4">
  <h2 class="text-center mb-4">Recipes for {{country}}</h2>

  <!-- Check if there are any recipes to display -->
  {{#if recipes.length}}
    <div class="row">
      {{#each recipes}}
      <div class="col-md-4 mb-4">
        <div class="card h-100">
          <div class="card-body">
            <h5 class="card-title">{{name}}</h5>
            <p class="card-text">{{description}}</p>
            <ul class="list-group list-group-flush">
              <li class="list-group-item"><strong>Prep Time:</strong> {{prep_time}} minutes</li>
              <li class="list-group-item"><strong>Cook Time:</strong> {{cook_time}} minutes</li>
              <li class="list-group-item"><strong>Servings:</strong> {{servings}}</li>
              <li class="list-group-item"><strong>Difficulty:</strong> {{difficulty}}</li>
            </ul>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  {{else}}
    <p class="text-center">No recipes found for {{country}}.</p>
  {{/if}}
</div>